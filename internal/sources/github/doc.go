// Package github implements the GitHub source collector for SignalForge.
//
// The package keeps API transport, response models, parsing, and collection
// orchestration separate so tests can isolate each concern without network
// access.
package github
